# Copia questo file in .env.local e riempi i valori (solo per sviluppo locale)
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=la-tua-anon-key-pubblica

# Questa NON va mai nel frontend: si configura solo su Vercel come
# variabile d'ambiente del progetto, mai in questo file se il repo è pubblico
ANTHROPIC_API_KEY=sk-ant-xxxxx
